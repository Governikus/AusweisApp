ADD_PLATFORM_LIBRARY(AusweisAppGlobal)

TARGET_INCLUDE_DIRECTORIES(AusweisAppGlobal SYSTEM PUBLIC ${OPENSSL_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(AusweisAppGlobal Qt5::Core ${OPENSSL_LIBRARIES} ${OSX_SECURITY})
